- defaults:
    shell:
        stdout: true
        stderr: true

- shell:
    -
        command: 'brew --version || /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"'
        description: Setting up brew
