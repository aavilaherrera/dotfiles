- defaults:
    shell:
        stdout: true
        stderr: true

- shell:
    -
        command: 'brew --version || { mkdir Homebrew && curl -L https://github.com/Homebrew/brew/tarball/master | tar xz --strip 1 -C Homebrew }'
        description: Setting up brew locally
